@using WebSiteBanSach.Models
@model List<ChiTietDonHang>

@{
    ViewBag.Title = "ChinhSua";
    Layout = "~/Views/Layout/NguoiDungLayout.cshtml";
}

<h2>Chỉnh sửa thông tin chi tiết đơn hàng</h2>

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()

    <div class="form-horizontal">
        <h4>Đơn Hàng</h4>
        <hr />
        @Html.ValidationSummary(true)

        <div class="form-group">
            @Html.LabelFor(modelitem => Model.First().MaDonHang, new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                <span style="font-size:15pt;margin-top:2px;margin-left:5px">@Model.First().MaDonHang</span>
                @Html.Hidden("MaDonHang", Model.First().MaDonHang)
            </div>
        </div>
        <br />
@foreach (var item in Model)
{
        <div class="form-group">
            @*@Html.LabelFor(modelitem => item.Sach.TenSach, new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                <span style="font-size:15pt;margin-top:2px;margin-left:5px">@item.Sach.TenSach</span>
                @Html.Hidden("MaDonHang", item.Sach.TenSach)
            </div>*@
        </div>

        <div class="form-group">
            @Html.LabelFor(modelitem => item.SoLuong, new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.EditorFor(model => item.SoLuong)
                @Html.ValidationMessageFor(model => item.SoLuong)
            </div>
        </div>
}
        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Lưu" class="btn btn-default" />
            </div>
        </div>
    </div>
}

<div>
    @Html.ActionLink("Back", "LichSuMuaHang")
</div>
